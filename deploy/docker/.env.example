# Copier en .env (ne pas commiter .env)
FRAPPE_SITE_NAME=erp.topcenter.cg
MYSQL_ROOT_PASSWORD=change_me
VERSION=v1.0.0

# ADMIN_PASSWORD est pass? au runtime install.sh, pas stock? en clair.
# Exemple:
# ADMIN_PASSWORD='StrongPass!' ./install.sh --site erp.topcenter.cg --modules core,hr --demo no
