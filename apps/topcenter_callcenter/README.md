# TopCenter Call Center

Module **à la carte** : Call Center.

- Dépend de **core**. Optionnel.

Installation : `bench --site <site> install-app topcenter_callcenter` ou via `install.sh --modules core,callcenter`.
